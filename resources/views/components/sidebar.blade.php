    {{-- <aside class="sidebar">
        <button id="closeSidebarButton">Close Sidebar</button>
        
        <ul>
            @foreach ($articles as $article)
            <li><a href="{{route('article.byCategory',['category'=>$article->category->id])}}">{{$article->category->name}}</a></li>
            @endforeach
        </ul>
    </aside> --}}